<UserControl x:Class="MusicBand_Manager.View.LyricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicBand_Manager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding LyricsText}" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto">
        </TextBox>
        <Button Content="Save Changes" Command="{Binding SaveChangesCommand}" HorizontalAlignment="Right" />
    </StackPanel>
</UserControl>
